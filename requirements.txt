# ── Core Framework ──
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0

# ── Async Database ──
sqlalchemy[asyncio]>=2.0.30
aiosqlite>=0.20.0      # SQLite async driver (swap to asyncpg for PostgreSQL)
alembic>=1.13.0

# ── Redis (cache + pub/sub) ──
redis[hiredis]>=5.0.0

# ── HTTP Client ──
httpx>=0.27.0

# ── Auth & Security ──
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# ── Logging ──
loguru>=0.7.0

# ── AI / ML (uncomment based on your models) ──
# torch>=2.2.0
# torchaudio>=2.2.0
# transformers>=4.40.0
# diffusers>=0.28.0
# TTS>=0.22.0

# ── Utils ──
python-multipart>=0.0.9
python-dotenv>=1.0.1
aiofiles>=23.2.1

# ── Testing ──
pytest>=8.2.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
